PORT=3001
ADMIN_TOKEN=change-me-in-production
# Comma-separated browser origins allowed for CORS (shell URL). Empty = allow all (dev only).
WEB_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
# SQLite file (default ./data/quest.sqlite under cwd)
# SQLITE_PATH=/data/quest.sqlite
