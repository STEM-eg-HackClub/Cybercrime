# Optional: full API origin in production if shell is not same host as API
# VITE_API_BASE=https://api.yourdomain.com

# WebGL export URL (same-origin: place export under public/game/)
VITE_GAME_URL=/game/CyberCrime.html
